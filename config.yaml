data:
  data_dir: data/real_vs_ai
  output_dir: models

model:
  model_name: resnet18
  pretrained: true
  image_size: 224

training:
  epochs: 10
  batch_size: 32
  learning_rate: 0.0001
  num_workers: 2

runtime:
  torch_compile: true
  compile_mode: default
  compile_backend: inductor
